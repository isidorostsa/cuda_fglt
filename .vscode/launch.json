{
    "version": "2.0.0",
    "configurations": [
        {
            "preLaunchTask": "build",
            "name": "CUDA C++: Debug",
            "type": "cuda-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/bin_debug/fglt",
            "args": "10"
        }
    ]
}